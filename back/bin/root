#!/usr/bin/env node

/**
 * Module dependencies.
 */

var Server = require('../app').NodeServer;
var debug = require('debug')('testServer:server');

const server = new Server();
server.start_server();
server.init_middleware();
